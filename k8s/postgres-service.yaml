apiVersion: v1
kind: Service
metadata:
  name: oficina-postgres
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app: oficina-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432